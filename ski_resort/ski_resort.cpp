#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n; // array length
        int k; // min days
        int q; // max temp

        cin >> n >> k >> q;
        int a[n];
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }

        int count = 0;
        


        cout << count << '\n';

    }
}